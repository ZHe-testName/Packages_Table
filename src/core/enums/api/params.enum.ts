export enum PACKAGE_TYPES {
  GH = 'gh',
  NPM = 'npm',
};

export enum SORT_BY {
  HITS = 'hits',
  BANDWIDTH = 'bandwidth',
}; 

export enum PERIOD_TYPES {
  DAY = 'day',
  WEEK = 'week',
  MONTH = 'month',
  QUARTER = 'quarter',
  YEAR = 'year',
  LAST_MONTH = 's-month',
  LAST_QUARTER = 's-quarter',
  LAST_YEAR = 's-year',
};
